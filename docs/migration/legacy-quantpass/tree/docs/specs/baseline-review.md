# 原型复用、冲突与技术债

基线提交：51a019fc81ba0c4ae1cd15eb8a10bc03cb42f759；2026-09-05 检查。当前交付是在该基线上加入单人计划、HTML 看板、模块入口与 M00 规格。

## 可复用 / 不可直接带入生产

| 现有对象 | 可复用内容 | 未实现或需要替换 | 承接 |
|---|---|---|---|
| src/security-model/index.ts | 整数字符串金额、签名负向测试、policy hash、短时许可和目标差量思想 | Ed25519 非 EIP-712；缺少链上 Vault/资产最终执行约束 | M02/M05/M06 |
| MemoryReplayStore | 重放拒绝测试与存储接口思路 | 进程内状态不能证明持久化、并发、重组正确性 | M03/M08 |
| TrustedStrategyRelease | 签名者/measurement 绑定样例 | 手工信任条目不是 attestation；没有解密系统或不可变端到端校验 | M04/M09 |
| test/security-model.test.ts | 原 6 个回归用例保留 | Binance/BTC/ETH、杠杆等是旧测试样例，非批准产品范围 | M01/M07 |
| HTML 模块看板及其 7 个测试 | 本机进度工具、JSON 备份、计划结构检查 | 无登录/业务 API/钱包；浏览器标记不等于项目实现进度 | M01 工具维护；不计 M03 完成 |

## 文档冲突处置

| 冲突 | 本次处理 | 仍开放 |
|---|---|---|
| 旧 Product Model 把 fixed supply、eligible releases 当作确定对象 | 改为待确认供应机制、版本权限候选，不授予升级权限 | D1/D4 |
| ReferenceVault 被称为唯一 canonical 方法 | 明确它仅为候选；公开业绩不能计入未运行持有人 | D5 |
| 额度按 capacity policy 当前价值变化 | 对齐每 Pass 1 USDT 额度；多存/闲置/浮盈例外显式描述 | D2/D5 |
| FeePolicy timelock 仿佛允许事后改费 | 标记机制待确认，不假定 timelock 即获用户许可 | D5 |
| 白皮书含升级访问权、固定供应、Reference Vault、HWM 等旧确定措辞 | M00 后续直接修正文中权限/供应/业绩/计费表述，并对齐 M00–M16 路线；TEE 等仍为候选方向 | D1/D4/D5，不宣称全文已定稿 |
| 原型 authorization 有到期时间，Pass 永久 | 临时交易许可与永久 SaaS 权利是不同对象，不用许可过期推导 Pass 过期 | 永久服务保障 D1/D6/D7 |

## 不在本次完成的任务

不选择前后端框架或 token 标准、不搭实际账本、不重新实现签名、不采购 TEE、不核验外部资产/法律事实。M01 负责可复现工程工具；M02 负责真实领域状态机；M04 取得有日期的官方事实证据；M16 仍需独立安全/法律/运维验收。

本次修改仅整理当前决策和样例，不能把历史风险“已分配到模块”当作风险已经修复。

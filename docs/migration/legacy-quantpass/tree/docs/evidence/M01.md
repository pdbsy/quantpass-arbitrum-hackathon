# M01 工程基线证据

日期：2026-09-05。状态：本地验收通过；GitHub workflow 写入权限阻塞，远端 CI 未运行。前置：M00 已由用户确认基础规格；技术选型由用户授权助手总览后决定，见 ADR-0001。

## 交付

- M01.01：[ADR](../adr/0001-project-foundation.md)、Node/npm 基线、精确依赖与锁文件、[安装说明](../ENGINEERING.md)。
- M01.02：统一 typecheck/lint/format/test；共享 packages/config。业务领域包按需留到 M02，未创建空壳或把环境变量规则塞进资金领域。
- M01.03：只读权限且 SHA 锁定的 CI、常见 secret 模式检查、npm audit、构建 manifest、无真实凭据的 .env.example、拒绝生产配置。远端执行尚待下方记录。
- M01.04：check、verify:gates、verify:clean、[证据模板](TEMPLATE.md)。

## 实际本地结果

环境：Windows，Node v24.12.0，npm 11.6.2，TypeScript 6.0.3。源版本以包含本证据文件的 Git 提交为准；本地构建 manifest 会区分有未提交修改的运行。

| 命令 | 实测结果 |
|---|---|
| npm run check | 通过；typecheck、lint、format、23 个单测、secret 基线与 build 均退出 0 |
| npm run verify:gates | TS2322、故意失败测试、生产/mock、production NODE_ENV、缺失模式均按预期拒绝；合成 token 被检测 |
| npm run verify:clean | 最终快照在全新 .checks/clean-WYSF85 安装成功；完整 check（23 个测试）和 verify:gates 均通过，无原 node_modules 复用 |
| npm run audit:dependencies | 注册表查询成功，0 个已知漏洞；仅为查询时点结果，不证明零风险 |

发现并修复：原类型检查缺 Node 类型与 .ts 导入选项；原 TypeScript 7 超出 lint 支持范围；现有浏览器 catch 未使用变量；Windows/Linux 换行影响生成看板 hash。原 6 项风险许可测试保持通过，未改变其业务行为。

## 远端状态

M01 实现提交：162202eaeed20d64ae1d39b14efd9a4f1ce0ca27。推送被 GitHub 拒绝：当前 OAuth 凭据缺少 workflow scope，不能创建或更新 .github/workflows/ci.yml。未运行远端 CI，M01.03 与整模块不标记完成。没有删去 workflow、改写提交历史或寻找其他凭据绕过限制；需要用户调整 GitHub 授权后重试原提交。

## 限制与未执行项

- React/Vite/Fastify 是已选工程方向，实际业务应用在 M03 创建；未运行产品 E2E。
- 配置检查目前是共享函数/CLI，不是已经部署的生产启动保护。未来入口须接入并补测试。
- Secret 基线只覆盖常见模式、Git 跟踪/未忽略文本；不是完整历史扫描、助记词检测、Gitleaks 或独立审计。
- 未启用分支保护/强制合并规则，CI 失败目前不能从仓库设置层阻止所有直接推送；更改仓库管理策略需另行确认。
- 没有真实资金、网络交易、客户策略、生产凭据或新增付费服务；M16 外部验收仍开放。
- .checks 中保留了仅本轮生成的干净安装/故障注入目录，Git 忽略；未删除任何用户资料。

下一步：核验最终干净快照和远端 CI，再确认 M01 完成；M02 尚未启动。

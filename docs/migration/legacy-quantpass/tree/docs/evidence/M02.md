# M02 本地领域模型验收

日期：2026-09-05；状态：本地模拟模块自测完成，D2/D5 生产规则仍开放，M01 远端 CI 权限阻塞未解除。

输出：packages/domain/src/money.ts、vault.ts；modules/M02/fixtures.ts；test/domain.test.ts；[规范与局限](../specs/domain-ledger.md)。

- M02.01：规范整数金额、精度转换、Pass SaaS 额度，未绑定市场价。
- M02.02：闲置/运行现金/订单预留/持仓/待提款/费用负债及前后置条件。
- M02.03：显式全量结算政策接口，未实现浮盈不当现金，费用与超额规则仅 fixture。
- M02.04：12 个新测试；全仓库 35/35 通过；包含 1,500 次确定性状态序列、每步守恒与恢复验证。

实测命令：npm run check（typecheck、lint、format、35 个测试、secret 基线、构建均通过）。环境：Node v24.12.0，npm 11.6.2。开启 erasableSyntaxOnly，让编译器提前拒绝 Node strip-only 模式不支持的 TS 语法。

已测拒绝：超额度、闲置不足、错误 owner/执行角色、重复订单成交、取消后提款确认、旧 revision、同 id 不同请求、未配置结算政策、无效费用与超额输出、坏快照。失败保留原对象，重试记录可随 JSON 恢复。

不是独立审计、真实持仓对账或生产资金批准；多资产/部分成交/链重组/真实所有权等仍见规范限制。下一项：M03 本地 API、持久存储和演示页面。

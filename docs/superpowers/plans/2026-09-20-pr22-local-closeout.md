# PR22 Local Closeout Implementation Plan

> **For agentic workers:** Use executing-plans with the existing user-owned Macbeth02–06 tasks. Do not create workers. Checkboxes reflect completed work only.

**Goal:** Finish engineering and reproducible acceptance. On 2026-09-21 the user made the repository public and explicitly restored normal PR validation; 01 coordinates hosted execution on the accurate final candidate. Merge/deployment remain unauthorized.

**Architecture:** Preserve the current candidate and original worker histories. A versioned source-coverage entrypoint consumes a fixed isolated tool graph, complete source inventory and real local workflow artifacts, then rejects inconsistent evidence before counter union. Separate local CI evidence from hosted context and independent governance.

**Tech Stack:** Node 24.21.0 / npm 11.19.1; existing root lock and contract/scanner tools; isolated qualified Istanbul instrument 6.0.3, source-maps 5.0.6 and Playwright Core 1.62.1. No floating tools, installation scripts or new services.

**Spec:** docs/management/specs/PR22-LOCAL-CLOSEOUT-2026-09-20.md

## Global constraints

- SOURCE_BASE 3a78e34ba933f1e3239424d3bf0b1e27b1a65bb8; local base master 18f5352070910a867b9729b031aa2e3951785e01. New candidates receive new evidence.
- Local/mock only. Normal PR validation is reauthorized by the public-repository decision; publish only after reviewing the exact outgoing history. No new costs, credentials, permissions, merge or chain transactions.
- Preserve all findings, raw failures and absent-platform NOT_RUN. No fabricated context, result or denominator exclusion.
- Independent writable workspaces/dependencies/data. Local commit/ref or bundle handoff only.

## Task 1: Pause and source matrix (01)

- [x] Verify local origin/branch/HEAD/tree/clean state.
- [x] Save current user specification, CI-BUDGET-PAUSE and update existing task JSON/Markdown/current status/resumption.
- [x] Dispatch existing 02–06 with exact scope and no hosted calls; record real ACK separately.
- [x] Commit the scope record using Agent-ID Macbeth01 / Task-ID M3-01-PHASE1-CLOSEOUT.

## Task 2: Coverage qualification and executable evidence (01, independently 05)

**Files:** tools/coverage/{evidence,prototype-map,inventory,toolchain,collect,report}.mjs; test/coverage-evidence.test.mjs; planning/coverage-toolchain.lock.json; docs/management/phase1/COVERAGE-QUALIFICATION.md. Package scripts stay manager-owned.

**Interface:** Manifest binds commit/tree/base, tool/config digests and every source/alias SHA plus static statementMap/fnMap/branchMap with zero counts. Observation binds the same identities, a unique lifecycle ID, completeness, actual command/exit/log hash and observed counters. Report records raw inputs, complete denominator, skipped/incomplete lifecycles and four separate metrics. Local evidence never asserts hosted PASS.

- [x] Write behavioral tests: complementary [1,0]+[0,1] yields [1,1]; never-loaded file stays [0,0]; raw input unchanged; altered commit/tree/tool/source/graph/IDs/path/count length, negative/fractional/unsafe counts and duplicate lifecycle reject; incomplete lifecycle contributes zero; aggregation overflow rejects.
- [x] Run tests against an explicit stub, retain red output; implement strict validator and rerun green.
- [x] Promote independently tested prototype mapping: exact single script, every UTF-16 coordinate through style insertion, missing/ambiguous script rejects, generated bytes must match importer.
- [x] Freeze complete Git inventory and verify source bytes. Type-only requires AST proof. Preserve zero-counter module entries. Deduplicate forum copy only with byte identity, explicit generator provenance and stale-check proof.
- [x] Version exact tool graph, license/source/artifact provenance and installed-byte verification. Unknown/missing/changed tool graph fails closed; no root product dependency change.
- [ ] Expose prepare/collect/report commands with unique run directories, actual exit status, bounded processes and preserved raw reports. Capture Node tests/CLI lifecycles and browser real workflows in an isolated instrumented build. Self-instrument first-party measurement code rather than silently removing it.
- [ ] Exercise command success/failure, stale candidate/source/map, missing log, wrong tool and unflushed child negatives. Final acceptance runs only on committed clean source with recorded environment.
- [ ] 05 independently reviews method/source classifications, reproduces exact final candidate and closes P1-001 only when threshold and frozen critical semantic obligations are met.

## Task 3: Product race and actual gaps (02–04; 05 independent)

- [ ] 04 reproduce review A → confirm → suspended awaited check → select B on same account/chain → resume; cover action, Pass transfer and deposit approval plus A→B→A, double click, refresh and late callback.
- [ ] If reproduced, add failing behavior regressions then guard the actual pre-wallet-send boundary; after send retain original operation identity. Do not describe an issued request as cancelled.
- [ ] 03 supplement chain-store/recovery/lease/multi-Vault behavior from exact gap data, retaining already-closed critical cases; run real backup/restore workflow.
- [ ] 02 provide exact contract/compiler/ABI/source equality and necessary local gate/deployment rehearsal, preserving core vs full-compiler coverage scopes.
- [ ] 04 add real browser paths and assertions for uncovered UI behavior; 05 independently retests fixes at exact SHA.

## Task 4: Local CI and external preparations (06, 01)

- [ ] 06 maps nine jobs, researches official stable Woodpecker/Jenkins/act/platform/fees and implements tools/local-ci/** with negative PoC tests. Only trusted local source, no new service or identity.
- [ ] 05/06 use saved Windows logs and bounded qualified native environment if present; otherwise native run NOT_RUN and root cause UNDETERMINED.
- [ ] 01/06 prepare source-bound external gate/review/reconnect permissions and rollback; do not execute them.
- [ ] Link four collector NOT_RUN entries to actual separate contract evidence without editing their results.

## Task 5: Integration and handoff (01)

- [ ] Inspect delivered worker commits/bundles, preserve parents/authors and update integration provenance without force push.
- [ ] Freeze new C, run actual local tests/scanners/coverage/browser/contract evidence, write R then generate S by existing protocol. No old hosted PASS for new source.
- [ ] Synchronize existing QA/findings/task/status facts and FINAL-EVIDENCE-INDEX; local bundle verifies against intended base/ref and hashes.
- [ ] Report accurate engineering/coverage/remote/security/governance/review/merge/Testnet states and the minimum external decisions. Keep NOT_READY_TO_MERGE until all actual protected conditions hold.

# Phase 1 Chain Recovery Gap Matrix

Status date: 2026-09-20
Owner: Macbeth03 / `M3-03-PHASE1-RECOVERY`
Base: `18f5352070910a867b9729b031aa2e3951785e01`
Runtime boundary: Local / Mock / NOT_DEPLOYED

`PASS_LOCAL` means the cited behavior passed locally with Node 24.21.0. It is not Hosted CI, independent review, deployment, or Testnet evidence. `HANDOFF_READY` means the interface is precise but its product-side consumption belongs to Macbeth04. `NOT_RUN` means the external-chain action was deliberately not executed.

| Requirement                                     | Disposition                                       | Evidence and boundary                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Manifest environment and chain ID               | PASS_LOCAL                                        | `test/chain-rpc-manifest.test.ts` validates the exact environment, chain `46630`, digest, and strict field set. `test/chain-sync.test.ts` rejects the wrong RPC chain before persistence.                                                                                                                                                                                            |
| Vault address and nonzero deployment target     | PASS_LOCAL                                        | `test/chain-rpc-manifest.test.ts` binds the expected address and rejects the zero address. Product submissions are dispatched only to a configured chain/contract pair in `test/chain-api.test.ts`.                                                                                                                                                                                  |
| ABI identity                                    | PASS_LOCAL                                        | Vault and StrategyPass ABI hashes are mandatory and digest-bound. `M3_VAULT_ABI_HASH` comes from the frozen Vault handoff; the StrategyPass ABI hash is bound to Macbeth02 source `2ad816200e7edfbfad96d765b4a696bc8b838c2d` and evidence head `a13052993b6f408b7be835ecd6f4b13ef6df367d`. Codec tests verify exact selectors/topics and reject trailing or malformed words.                                      |
| Live runtime bytecode identity                  | PASS_LOCAL                                        | `ReadonlyRpc.code` allowlists `eth_getCode`; `M3ChainRuntime` verifies nonempty Vault and StrategyPass code and each deployed Keccak-256 before indexing. `test/chain-runtime.test.ts` proves a mismatch persists no checkpoint. No deployed code exists to verify externally.                                                                                                       |
| Wallet/config/RPC chain identity                | PASS_LOCAL                                        | Backend checks manifest, RPC chain, address, ABI, and code. Existing `test/ui-chain-wallet.test.ts` and `test/ui-m3-vault-live-reader.test.ts` cover wrong-chain and mid-read session changes on the browser side.                                                                                                                                                                   |
| Target, calldata, and value                     | PASS_LOCAL                                        | `M3ChainRuntime.recordSubmission` accepts only exact configured Vault owner calldata or exact `transfer(address,uint256)` to its manifest-bound StrategyPass. `test/chain-runtime.test.ts` rejects view, arbitrary calldata, and arbitrary token targets; browser action tests require zero native value.                                                                            |
| Spender and finite amount                       | PASS_LOCAL / HANDOFF_READY                        | Existing `test/ui-m3-vault-allowance.test.ts` fixes AF-USDC and Pass approval spender to the selected Vault and uses exact finite raw amounts. Macbeth04 requirements are recorded in `PHASE1-MACBETH04-API-HANDOFF.md`.                                                                                                                                                             |
| Immutable nonzero Owner                         | PASS_LOCAL from merged contract evidence          | `contracts/src/AlphaForgeVault.sol` rejects zero Owner and stores an immutable Owner. Canonical reconciliation reads Owner at one block and matches it to the operation; `test/chain-vault-integration.test.ts` covers the binding. Runtime code verification binds reads to that reviewed implementation.                                                                           |
| Strategy, Pass, and Vault identity              | PASS_LOCAL                                        | Canonical reads bind the Vault address, strategy ID, Pass address, and `StrategyPass.strategyId()` at the same block. A mismatch fails in `test/chain-vault-integration.test.ts`.                                                                                                                                                                                                    |
| Rejection, revert, replacement, and drop        | PASS_LOCAL                                        | Explicit lifecycle transitions and evidence are covered by `test/chain-lifecycle.test.ts` and `test/chain-sync.test.ts`. A tx hash remains only `SUBMITTED`.                                                                                                                                                                                                                         |
| Wallet disconnect and account/chain changes     | PASS_LOCAL from merged browser evidence           | `test/ui-chain-wallet.test.ts`, `test/ui-m3-vault-allowance.test.ts`, and `test/ui-m3-vault-live-reader.test.ts` fail closed when provider identity changes or disconnects. Backend never derives authority from a demo session.                                                                                                                                                     |
| `SUBMISSION_AMBIGUOUS`                          | PASS_LOCAL / browser-owned                        | Browser tests make every ambiguous result non-retryable. The backend submission schema requires a concrete tx hash and rejects an ambiguous status without creating an operation in `test/chain-api.test.ts`; no automatic resend path exists.                                                                                                                                       |
| Duplicate callback / repeated observation       | PASS_LOCAL                                        | Reposting one operation identity is idempotent, conflicts fail closed, and duplicate logs remain one event across restart in `test/chain-api.test.ts` and `test/chain-store.test.ts`.                                                                                                                                                                                                |
| Block, parent, log, receipt, and event identity | PASS_LOCAL                                        | `test/chain-sync.test.ts` covers wrong contract/block logs, fork ancestry, receipt/event mismatch, and projection rebuild rollback.                                                                                                                                                                                                                                                  |
| Canonical ordering and same-block operations    | PASS_LOCAL                                        | Store ordering is block → transaction index → log index. `test/chain-vault-integration.test.ts` reconciles earlier same-block deposit/withdraw after a later close without replacing the final projection.                                                                                                                                                                           |
| Checkpoint/restart recovery                     | PASS_LOCAL                                        | Checkpoints contain height and hash; restart/replay recovery is covered by `test/chain-sync.test.ts` and `test/chain-startup.test.ts`.                                                                                                                                                                                                                                               |
| Cross-process sync ownership                    | PASS_LOCAL                                        | SQLite sync leases prevent stale or lower-head synchronizers from clearing newer work in `test/chain-sync.test.ts`.                                                                                                                                                                                                                                                                  |
| Bounded catch-up                                | PASS_LOCAL                                        | Startup scans at most the configured batch and keeps projections unavailable until the actual head in `test/chain-startup.test.ts`.                                                                                                                                                                                                                                                  |
| Reorg within and beyond 128 blocks              | PASS_LOCAL                                        | Default `reorgSearchLimit = 128`. Recoverable forks rewind to a verified ancestor; excessive/no-ancestor cases preserve evidence and enter degraded/manual recovery in `test/chain-sync.test.ts`.                                                                                                                                                                                    |
| Degraded indexer and Owner exits                | PASS_LOCAL from merged cross-layer evidence       | Backend projection reads fail closed. Existing `test/m3-browser-runtime.test.ts` and `test/m3-product-ui.test.ts` preserve independently verified Owner withdraw/close actions while preventing stale READY claims.                                                                                                                                                                  |
| Multi-wallet isolation                          | PASS_LOCAL                                        | `test/chain-store.test.ts` proves wallet projections stay separate and do not imply product-account ownership.                                                                                                                                                                                                                                                                       |
| Multi-Vault and multi-strategy isolation        | PASS_LOCAL                                        | `test/chain-startup.test.ts` starts distinct-Pass runtimes and two Owner/Vault runtimes sharing one StrategyPass through the real `startM3Server` path. Vault state, PassLocker, exit operations, and SQLite files remain isolated; the lexically first Vault owns shared-Pass sync/storage/routes, conflicting Pass identity fails closed, and degraded owner sync makes cached Pass reads unavailable. `test/chain-api.test.ts` verifies explicit contract dispatch; ambiguous owner-only selection is rejected as `INVALID_REQUEST`. |
| Database migrations                             | PASS_LOCAL                                        | `test/chain-store.test.ts` migrates version 1 and version 3 evidence to schema 6 and rejects unrelated/unsupported databases.                                                                                                                                                                                                                                                        |
| Database backup and restore                     | PASS_LOCAL                                        | `ChainStore.backupTo` performs an online backup, reserves the target exclusively, and never overwrites it. `test/chain-store.test.ts` preserves evidence and runs a three-sample local recovery drill with separately reported backup, reopen/health, and 128-block catch-up times. The measurement is explicitly not a Testnet SLA. |
| Database health and diagnosis                   | PASS_LOCAL                                        | `ChainStore.health()` reports fixed status/schema/integrity fields. `/api/v1/chain/runtime-status` exposes no database path, SQL, RPC URL, or private error. See `PHASE1-DATABASE-RECOVERY.md`.                                                                                                                                                                                      |
| Initial Pass allocation                         | PASS_LOCAL contract evidence / deployment NOT_RUN | StrategyPass fixed supply is minted once to constructor `recipient_`; there is no post-deployment mint. The source and artifact hashes are recorded from Macbeth02 source `2ad816200e7edfbfad96d765b4a696bc8b838c2d`; concrete constructor values and deployment remain unauthorized and NOT_RUN.                                                                                     |
| Ordinary Pass transfer                          | PASS_LOCAL / HANDOFF_READY                        | Exact ERC-20 `transfer(address,uint256)` preserves all 18-decimal raw units, including one raw unit. One deterministic synchronizer per `(chainId, StrategyPass)` indexes logs, reconciles sender/recipient/amount, reads canonical holder balances, and owns `GET /api/v1/chain/passes/:contract/:owner` plus Pass operation evidence even when multiple Vaults share the Pass. Macbeth04 owns product consumption. |
| Vault selection                                 | PASS_LOCAL / HANDOFF_READY                        | Backend supports contract-qualified status/read/submission. Macbeth04 must bind the selected strategy to an allowlisted manifest Vault rather than accept arbitrary client labels.                                                                                                                                                                                                   |
| Post-close rescue                               | PASS_LOCAL / HANDOFF_READY                        | Vault ABI supports `rescueUntrackedToken(address)` and `rescueNative()`. Reconciliation requires exact events, immutable Owner, and closed state in `test/chain-vault-integration.test.ts`.                                                                                                                                                                                          |
| Real Robinhood Chain Testnet smoke              | NOT_RUN                                           | Deployment manifest fields and addresses remain unset. No external read, signature, transaction, initialization, or broadcast was executed. Procedure is prepared in `PHASE1-TESTNET-SMOKE.md`.                                                                                                                                                                                      |

## Remaining dependencies

- Macbeth02/01 must publish concrete deployed StrategyPass address/block/runtime hash values for the final deployment manifest; the source and ABI identity are already pinned to `2ad816200e7edfbfad96d765b4a696bc8b838c2d`.
- Macbeth04 must consume the contract-qualified routes and add the approved Pass transfer, Vault selection, and rescue product controls in its owned `apps/web/**` files.
- Macbeth01 must integrate shared configuration and any package/CI changes, then designate the final candidate for Macbeth05/06 review.
- Real Testnet deployment and wallet actions remain blocked on separate deployment/signing/broadcast authorization and concrete immutable deployment evidence.

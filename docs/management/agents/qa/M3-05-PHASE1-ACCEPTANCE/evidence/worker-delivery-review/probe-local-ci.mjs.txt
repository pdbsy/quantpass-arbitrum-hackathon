import { runLocal, verifyRun } from '/private/tmp/AlphaForge-M3-05-LOCAL-CI-D1C52E3/tools/local-ci/runner.mjs';
import { mkdtempSync, mkdirSync, writeFileSync, readFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { execFileSync } from 'node:child_process';
import { createHash } from 'node:crypto';
const directory=mkdtempSync(join(tmpdir(),'af-qa-runner-'));
const cwd=join(directory,'source');mkdirSync(cwd);
const git=(...args)=>execFileSync('/usr/bin/git',args,{cwd,encoding:'utf8',env:{PATH:process.env.PATH,HOME:directory,GIT_CONFIG_GLOBAL:'/dev/null',GIT_CONFIG_NOSYSTEM:'1'}}).trim();
git('init','-b','qa-fixture');writeFileSync(join(cwd,'check.mjs'),'process.exit(7);\n');git('add','.');git('-c','user.name=QA Fixture','-c','user.email=fixture@example.test','-c','commit.gpgsign=false','commit','-m','failure fixture');
const expected={base:git('rev-parse','HEAD'),head:git('rev-parse','HEAD'),tree:git('rev-parse','HEAD^{tree}'),node:process.versions.node};
const config={cwd,outputRoot:join(directory,'runs'),expected,jobs:[{id:'source-check',executable:process.execPath,args:['check.mjs'],platform:process.platform,arch:process.arch,timeoutMs:3000}]};
const original=await runLocal(config);
const copied=join(directory,'negative-readback');mkdirSync(copied);
for (const name of ['report.json','report.sha256', 'source-check.stdout.log','source-check.stderr.log']) writeFileSync(join(copied,name),readFileSync(join(original.directory,name)));
const malformed=JSON.parse(readFileSync(join(copied,'report.json'),'utf8'));
malformed.state='PASS';malformed.reason='retained prerequisite reason';
const bytes=JSON.stringify(malformed,null,2)+'\n';writeFileSync(join(copied,'report.json'),bytes);writeFileSync(join(copied,'report.sha256'),createHash('sha256').update(bytes).digest('hex')+'\n');
const readback=verifyRun(copied,expected);
git('update-index','--assume-unchanged','check.mjs');writeFileSync(join(cwd,'check.mjs'),'process.exit(0);\n');
const hiddenStatus=git('status','--porcelain','--untracked-files=no');
const hidden=await runLocal(config);
git('update-index','--no-assume-unchanged','check.mjs');
git('checkout','--','check.mjs');
git('update-index','--skip-worktree','check.mjs');
writeFileSync(join(cwd,'check.mjs'),'process.exit(0);\n');
const skipped=await runLocal(config);
const incompleteDir=join(directory,'missing-fields');mkdirSync(incompleteDir);
for (const name of ['report.json','report.sha256', 'source-check.stdout.log','source-check.stderr.log']) writeFileSync(join(incompleteDir,name),readFileSync(join(hidden.directory,name)));
const incomplete=JSON.parse(readFileSync(join(incompleteDir,'report.json'),'utf8'));
for (const field of ['signal','timedOut','leftChildren','processFailure','logFailure','startedAt','finishedAt','pid']) delete incomplete.jobs[0][field];
const incompleteBytes=JSON.stringify(incomplete,null,2)+'\n';
writeFileSync(join(incompleteDir,'report.json'),incompleteBytes);
writeFileSync(join(incompleteDir,'report.sha256'),createHash('sha256').update(incompleteBytes).digest('hex')+'\n');
console.log(JSON.stringify({runnerCommit:'d1c52e32510f2efee5e2896a246f749b19cc22f1',original:{state:original.state,jobState:original.jobs[0]?.state,exit:original.jobs[0]?.exitCode},contradictoryReport:{state:malformed.state,jobState:malformed.jobs[0]?.state,exit:malformed.jobs[0]?.exitCode,readback},skipWorktree:{state:skipped.state,readback:verifyRun(skipped.directory,expected)},missingRequiredOutcomeFields:{readback:verifyRun(incompleteDir,expected)},hiddenSource:{gitStatus:hiddenStatus,headSource:git('show','HEAD:check.mjs'),executedSource:readFileSync(join(cwd,'check.mjs'),'utf8').trim(),state:hidden.state,readback:verifyRun(hidden.directory,expected)}},null,2));

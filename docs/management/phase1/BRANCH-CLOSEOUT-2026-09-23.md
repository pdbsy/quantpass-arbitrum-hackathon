> 历史阶段记录：下文单人/暂停发布范围已被用户本轮授权取代。当前执行与基线见 [PR22最终收口记录](PR22-READY-CLOSEOUT-2026-09-23.md)，保留本页旧候选证据，不将其当现状。

# 集中补测与本地交付 — 2026-09-23

归属：AlphaForge / Robinhood Chain Testnet / Hackathon / PR #22；Macbeth01 / M3-01-PHASE1-CLOSEOUT。Xlayer 独立。本轮按用户要求由01执行，未启动或派发 worker。

用户“暂不发布，保留本地交付”继续有效。目标是尽可能覆盖可达的正常、失败、恢复和并发行为；90%是门槛，不是停止补测的理由。

## 基线与实测增量

完整基线 S 61483052bda9db73378eed1ab767601411047ee9：行92.67%、语句90.44%、函数92.38%、分支9241/10743（86.01%），未覆盖1502个。完整清单及风险归属保存在本地 outputs/phase1-local-closeout/branch-closeout/baseline-6148305.json。

| 本地提交 | 新增原有分支命中 | 累计未变源码命中 |
|---|---:|---:|
| eda6f70 | 102 | 102 |
| e59da4b | 78 | 180 |
| 2626eaa | 37 | 217 |
| ffe6fbe | 49 | 249 |
| 4f31b28 | 101 | 350 |
| 17ee0ad | 38 | 388 |
| ecea670 | 10 | 398 |
| 2b052c5 | 12 | 410 |

以上是分批诊断，仅对相同源码摘要、分支位置和编号去重。累计减少 **410个未变源码的原有缺口**。redact.mjs 的17个旧命中因源码修复已退出累计；该文件的旧53个缺口也不再与新图混算。新源必须在统一候选重新测量，不能将这些数值写成最终覆盖率。

## 实际修复和新增行为验证

- 修复日志脱敏语法匹配的优先级缺陷：后方CLI参数曾可能遮住前方的敏感赋值。按最早敏感位置截断，7种语法的49个有序组合同时验证两个虚构敏感值均不外泄、无关前缀保留。原始RED和GREEN日志保留。
- SQLite重组、事件重新纳入与触发器注入的事务失败：失败不遗留部分块、检查点或事件；随后合法恢复成功。
- 客户端损坏回执、账户切换、HTTP-date限流和恢复：服务端已提交但回执损坏时，原请求不丢失，最终只有一次账目变更；不混用账户数据。
- 实际浏览器覆盖跨标签页冲突、真实30秒报价过期、买卖及容量边界、亏损归零、限流刷新后的身份确认与同请求重试，保留页面/CSP/手机布局检查。
- 管理看板与论坛真实脚本下的失败、空源、未知状态、缺字段、筛选、键盘和不安全链接；测试fixture不写入生成的实际看板证据。
- 钱包证据拒绝畸形envelope、错误地址/调用编码及非零native value，精确保持uint256提款与救援金额；治理文件边界与复核日期负例保留。
- 实际Semgrep与合约门禁、四种产物生成器的两轮逐字节再生成验证使用现有入口和锁定工具，不构造扫描结果或修改counter。

## 剩余清单和边界

最终验收后，本地 full-acceptance-receipt.json 与 remaining-branches.json 提供准确候选、四维指标及每个剩余位置。整个分母保留，未对 App.tsx 的94个未使用入口分支添加忽略；未调用被实际入口替换的旧函数以凑数。平台专属执行需要对应真实宿主，当前Mac结果不冒充Windows/Linux通过。其余未命中保留 OPEN_UNCOVERED，不推定不可达。

统一验收顺序：源C → 实际management:checks → 仅清单R → management:build → 仅快照S → 同一S完整9工作流、原生Semgrep/合约/产物再生成、正式fnm与LOCAL身份核验。完成结果放入与准确S绑定的本地交付目录；此源记录不预填未来PASS。

整体数值、冻结关键权限/会计语义和独立方法准入分别判断。既有通过的关键子集保持其原候选范围；新脱敏修复和最终统一候选仍需要独立复核。发布暂停、准确新头托管验证、独立安全/治理/适格review及本轮merge授权未关闭。NOT_READY_TO_MERGE；LOCAL/MOCK/NOT_DEPLOYED，无推送、规则变更、签名或广播。

# 来源与浏览器恢复边界回归 — 2026-09-23

归属：AlphaForge / Robinhood Chain Testnet / Hackathon；pdbsy/quantpass-arbitrum-hackathon；PR #22；Macbeth01 / M3-01-PHASE1-CLOSEOUT。用户“暂不发布，保留本地交付”的限制继续有效；不涉及 Xlayer。

最近完整候选 d57a53ba654c4caa17bbac39cfca3f1165d28fe2 已通过956/956与8条实际功能工作流，Semgrep、OSV、Gitleaks、原生合约入口及正式fnm准入。完整报告SHA-256为39d41184498129179709fbd3d372a7c2b0c48ede4b77d5ffa375b69a8d826a4a；行91.54%、语句88.92%、函数91.53%、分支83.67%。其本地完整历史bundle已验证；不是本轮后续代码的验证结果。

本轮增加实际可验证的拒绝与恢复路径：

- 隔离Git图验证push/workflow_dispatch、PR父提交、merge-group、缺失/过期引用与脱离分支；真实观察器读取受控事件文件，验证跨仓库、错误base、缺失/非法JSON。所有这些只是本地测试夹具，不是GitHub托管证明，eligibleForEvidence仍false。
- 使用真实文件验证CRLF、非法UTF-8、大小上限、二进制NUL和符号链接；检查本地模式配置与npm路径错配。版本检查与平台路径检查分开断言，不能因版本正确就宣称工具运行路径正确。
- 直接验证真实提交的祖先、tree与仅清单变更的新鲜度；运行时更改不能冒充报告更新。数据源中的畸形Markdown、非文件和非目录明确失败。
- 安全模型无显式时钟时仍先校验授权；沿用离线测试签名，不接触真实钱包。
- 在原浏览器流程中验证v2/home旧存储规范化、浏览器读取拒绝、配额写入失败、可见导出、重置取消/确认、未知/错误编码/已删除笔记路由。拦截仅限隔离浏览器的本地存储API故障；不替换产品函数或覆盖计数器。
- 覆盖采集执行已有锁定OSV真实门禁，绑定当前candidate和raw输出；非零退出仍阻断完整功能集合，不把导入或历史扫描冒充命中。

工作树预检：环境/Git15项、来源绑定/安全模型108项通过，真实原界面浏览器262条具名模型/恢复检查与原有可见操作全部通过；typecheck/lint/format通过。初次夹具的祖先参数方向、字段引用、readonly时钟移除及旧history可选字段期望问题保留原始日志，修正夹具后通过；没有据这些夹具错误修改生产策略。

仅覆盖注册增加一条已有实际门禁；覆盖算法、完整源码分母、未使用入口、阈值、失败生命周期与正式准入规则不变。需要新的准确C/R/S和全量运行后再更新测量结论。NOT_READY_TO_MERGE；当前总体四维90%、正式方法/关键语义、新头托管验证、最终复核/治理和实际Testnet验收分别保持真实状态。

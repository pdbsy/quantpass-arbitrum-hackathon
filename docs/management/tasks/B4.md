# B4 Task Record

Task ID: `B4`

Title: `CREATE DASHBOARD ARCHITECTURE`

Worker: `Worker B / Macbeth`

Start: `2026-09-08T22:02:00+08:00`

Finish: `2026-09-08T22:19:53+08:00`

Status: `VERIFIED_DONE`

## Summary

Defined a source-driven, read-only QuantPass Control Center architecture and a task-by-task implementation plan. The design makes absence, source failure, stale evidence, unrun tests, risks, blockers, and user decisions explicit while keeping the browser and local server outside protocol and credential trust boundaries.

## Files changed

- `docs/management/dashboard/ARCHITECTURE.md`
- `docs/management/plans/2026-09-08-control-center.md`
- `docs/management/tasks/B4.md`
- `docs/management/workers/worker-b.md`

## Tests

- Repository file/source inventory completed.
- Existing task-board generator, CI, server, package scripts, roadmap, risk register, security boundary, and management records inspected.
- All 20 required Dashboard navigation areas mapped to authoritative sources or explicit unavailable states.
- Plan placeholder-pattern scan completed with no matches.
- Repository-local Git author and branch readback completed.

## Validation

The architecture defines closed task/check status vocabularies, a snapshot schema, source ownership, missing/error behavior, fixed Git/check execution boundaries, sanitization limits, safe browser rendering, a loopback-only server, atomic generation, and final branch review requirements.

## Known limitations

- Worker A, Manager status/control, decision, work-queue, and changelog sources do not currently exist.
- No persistent check-evidence report exists yet.
- Live GitHub CI cannot be queried by the static browser without expanding the trust boundary.
- The local runtime observed during this task did not satisfy the repository's declared minimum versions.

## Not fully resolved

- B5 homepage implementation.
- B6 source/test/Git/security integration.
- B7 automatic generation and serving.
- B8 full validation and handoff.
- SSH authorization cleanup decision.

## Deferred work

- Dashboard implementation proceeds through the recorded plan.
- Final branch merge remains deferred to Manager/user approval.

## Blockers

`NONE` for B5 implementation. Toolchain version mismatch must remain visible and may block clean-install reproducibility claims.

## Residual risks

- A committed Dashboard snapshot can become stale between builds.
- Markdown sources can drift from the documented parser contract.
- Redaction is defense in depth and cannot prove arbitrary logs are safe.
- The branch currently includes three prerequisite commits not yet present on `origin/master`.

## Decisions

- Static Dashboard plus build-time aggregation; no dynamic browser API.
- Same-origin text-only rendering under a restrictive CSP.
- Separate loopback server on `127.0.0.1:4181`; no LAN exposure.
- Fixed command registry and commit-bound evidence for test status.
- Missing optional data is visible; malformed mandatory data fails closed.
- Do not edit Manager-owned facts.
- Work on `macbeth/dashboard`, author commits as Macbeth, and do not merge without explicit approval.

## Commit

`SELF` — resolve with `git log -1 --format=%H -- docs/management/tasks/B4.md` after commit.

## Next recommended action

Execute Task 1 of the implementation plan with test-first development, then continue B5 only after schema and redaction tests pass.

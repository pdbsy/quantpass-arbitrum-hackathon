# B5 Task Record

Task ID: `B5`

Title: `CREATE CONTROL CENTER HOMEPAGE`

Worker: `Worker B / Macbeth`

Start: `2026-09-08T22:20:00+08:00`

Finish: `2026-09-09T00:04:55+08:00`

Status: `VERIFIED_DONE`

## Summary

Built a Chinese, source-driven QuantPass Control Center homepage. It presents project, branch, commit, phase, integration, security, checks, release gates, Worker states, task evidence, risks, blockers, links, network state, and unavailable sources without promoting missing or stale evidence to success.

## Files changed

- `docs/management/dashboard/index.html`
- `docs/management/dashboard/app.js`
- `docs/management/dashboard/styles.css`
- `test/management-dashboard-ui.test.mjs`
- `tools/build-management-dashboard.mjs`
- `test/management-dashboard-build.test.mjs`
- `docs/management/plans/2026-09-08-control-center.md`
- `docs/management/workers/worker-b.md`

## Tests

- UI structure and CSP test.
- HTML-injection sink exclusion test.
- Closed status and severity tests.
- Repository/fragment/loopback link allowlist tests.
- Explicit unavailable/blocker overview test.
- Fetch and schema-failure rendering tests.
- Combined Dashboard suite: 35/35 passed before this report update.
- ESLint and secret baseline passed.

## Validation

The static shell contains all 20 required navigation targets. Changing facts remain in generated JSON. Browser code renders source values through `textContent`, rejects unknown display states, and shows a generic `DATA_SOURCE_ERROR` without exposing exception details.

## Known limitations

- Browser and screen visual inspection was intentionally not performed because the user instructed Codex not to use the screen.
- Live local HTTP serving is not part of B5 and remains pending.
- Worker A, Manager control, decision, work-queue, and changelog sources do not exist and therefore render unavailable.
- CSS and DOM behavior are statically tested; cross-browser visual behavior is not yet evidenced.

## Not fully resolved

- B6 complete integration of management/task-report known-issue fields and remaining source links.
- B7 automatic refresh and loopback-only serving.
- B8 full project gate, security diff review, and handoff.

## Deferred work

- Live preview, server headers, traversal defense, and HTTP probes are deferred to B7.
- Manager-owned facts remain read-only and can only become available when their owners create the documented sources.

## Blockers

`NONE` for B6. Missing Manager-owned sources must remain visible but do not block read-only integration.

## Residual risks

- A static snapshot can become stale until regenerated.
- Static sink checks do not replace browser runtime testing.
- The renderer deliberately exposes detailed recorded security findings; redaction remains defense in depth rather than a secrecy proof.

## Decisions

- Use a stable HTML shell with generated JSON facts.
- Keep every failure, blocker, stale check, and unavailable source visible.
- Allow no source-driven HTML, CSS text, event handler, or arbitrary URL.
- Preserve a local-only preview boundary and do not broaden service exposure.
- Use `macbeth/dashboard` and Macbeth-authored commits; do not merge without explicit approval.

## Commit

`SELF` — resolve with `git log -1 --format=%H -- docs/management/tasks/B5.md` after commit.

## Next recommended action

Execute B6 source-completeness integration, especially task-report issue aggregation, Manager/decision availability, raw evidence links, and Dashboard diagnostics.

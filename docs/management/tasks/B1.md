# B1 Task Record

Task ID: `B1`

Title: `HOST BASELINE REPORT`

Worker: `Worker B`

Start: `2026-09-08T13:17:00+08:00`

Finish: `2026-09-08T13:41:34+08:00`

Status: `VERIFIED_DONE`

## Summary

Completed a read-only baseline of the Mac host, native OpenSSH service, firewall, SSH-key metadata, developer toolchain, project filesystem, and Git state. This status applies only to B1 evidence collection; the SSH host itself remains `BLOCKED` and is not accepted.

## Files changed

- `docs/management/host/HOST-BASELINE.md`
- `docs/management/tasks/B1.md`
- `docs/management/workers/worker-b.md`

## Tests

- Verified default LAN route and `en0` address.
- Verified TCP connection to port 22 through both loopback and the LAN address.
- Inspected launchd's native `com.openssh.sshd` socket configuration.
- Inspected application-firewall state.
- Checked SSH directory/key permissions without reading private key content.
- Executed version checks for Homebrew, Git, Node.js, npm, Codex CLI, and tmux.
- Verified the project path is on local APFS and not SMB.

## Validation

Evidence and interpretations are recorded in `docs/management/host/HOST-BASELINE.md`. No system or repository setting was modified before the report was produced.

## Known limitations

- `systemsetup -getremotelogin` requires administrator authorization.
- `sshd -T` could not read protected host private keys.
- A successful TCP connection is not authentication proof.

## Not fully resolved

- Firewall remediation.
- Windows public-key authorization.
- Windows SSH authentication.
- tmux installation and disconnect/reconnect verification.
- Remote Codex CLI execution.
- Effective password/root-login policy verification.

## Deferred work

- B2 host changes are deferred to the recorded approval checkpoint.
- B3 external-client verification is deferred until the Windows public key and client are available.

## Blockers

- macOS application firewall is disabled.
- `authorized_keys` is absent.
- `tmux` is absent.

## Residual risks

- TCP/22 is reachable while the application firewall is disabled.
- Password authentication policy is not yet proven.
- The data volume has approximately 13% capacity remaining.

## Decisions

- No configuration changes during B1.
- Treat launchd's non-running daemon state as normal on-demand behavior because the passive socket and TCP handshakes were independently verified.
- Do not store private keys, credentials, raw Codex data, or host public-key fingerprints in the public repository.

## Commit

`SELF` — resolve with `git log -1 --format=%H -- docs/management/tasks/B1.md` after commit; a Git commit cannot embed its own final hash.

## Next recommended action

Approve the exact B2 change set in the baseline report, then install tmux, enable the firewall, and authorize only a Windows-generated SSH public key while preserving password authentication until B3 succeeds.

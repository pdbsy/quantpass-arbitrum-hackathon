# B6 Task Record

Task ID: `B6`

Title: `INTEGRATE CONTROL CENTER SOURCES`

Worker: `Worker B / Macbeth`

Start: `2026-09-09T00:05:00+08:00`

Finish: `2026-09-09T00:16:01+08:00`

Status: `VERIFIED_DONE`

## Summary

Integrated the repository-owned project, management, Worker, task-report, security, check, Git, document, network, changelog, and SSH-history sources into the Control Center. Missing owner-controlled sources remain explicit `NOT_AVAILABLE`; malformed or escaping sources become `DATA_SOURCE_ERROR`.

## Files changed

- `tools/management-dashboard/sources.mjs`
- `tools/build-management-dashboard.mjs`
- `docs/management/dashboard/app.js`
- `docs/management/dashboard/styles.css`
- `test/management-dashboard-sources.test.mjs`
- `test/management-dashboard-build.test.mjs`
- `docs/management/workers/worker-b.md`

## Tests

- Task-report limitation, unresolved, deferred, blocker, and residual-risk aggregation.
- SSH host task/link integration.
- Full release-gate and current-wave aggregation.
- Document-directory containment and symlink escape rejection.
- Empty document-directory deterministic behavior.
- Source/build/UI integration-focused suite: 22/22 passed.
- Format, ESLint, secret baseline, and whitespace checks passed.

## Validation

The generated contract now contains detailed task acceptance/evidence, Worker activity, risk attack paths and mitigations, check commit/timing/evidence, Git history, all release gates, task-report issues, Manager-source envelopes, host history, and allowlisted document links. Source absence remains visible and does not become success.

## Known limitations

- `docs/management/CURRENT-STATUS.md`, `WORK-QUEUE.md`, `DECISIONS.md`, `CHANGELOG.md`, and Worker A's log are absent.
- Decision records cannot display structured decision fields until the owner supplies the documented source.
- Check evidence currently targets an ancestor commit and is therefore shown as stale/blocked.
- The local runtime still does not meet the repository's declared minimum Node/npm versions.

## Not fully resolved

- B7 loopback-only server and automatic validation wiring.
- Live local preview and HTTP security-header probes.
- B8 full gate, diff security review, and branch handoff.
- SSH key/service cleanup decision from the cancelled B3 workstream.

## Deferred work

- Manager-owned source creation remains with the Manager.
- Live CI provider status remains outside the static browser trust boundary.

## Blockers

`NONE` for B7. Missing optional sources and stale evidence must remain visible.

## Residual risks

- New task-report heading variants will be ignored until explicitly added to the conservative parser contract.
- Redaction cannot prove arbitrary source text contains no unknown secret format.
- Static Git and test observations become stale between explicit refreshes.

## Decisions

- Aggregate unresolved task-report sections instead of displaying only success paths.
- Resolve and contain allowlisted document directories before listing their files.
- Treat an empty document directory as `NOT_AVAILABLE` for deterministic generation.
- Keep B3 host status blocked/cancelled and do not infer SSH readiness from the existence of setup files.

## Commit

`SELF` — resolve with `git log -1 --format=%H -- docs/management/tasks/B6.md` after commit.

## Next recommended action

Implement and verify the loopback-only static server, then wire generation validation into the documented B7 workflow.

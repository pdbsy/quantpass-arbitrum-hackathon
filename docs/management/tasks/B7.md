# B7 Task Record

Task ID: `B7`

Title: `AUTOMATE GENERATION AND LOOPBACK SERVING`

Worker: `Worker B / Macbeth`

Start: `2026-09-09T00:18:00+08:00`

Finish: `2026-09-09T00:37:26+08:00`

Status: `VERIFIED_DONE`

## Summary

Completed the Control Center operational boundary: deterministic generated-file ownership metadata, a native read-only loopback server, DNS-rebinding protection, safe operator documentation, non-mutating main-gate validation, and execution of Dashboard regressions by both the ordinary test command and recorded unit evidence.

## Files changed

- `tools/serve-management-dashboard.mjs`
- `tools/build-management-dashboard.mjs`
- `tools/management-dashboard/schema.mjs`
- `tools/management-dashboard/checks.mjs`
- `test/management-dashboard-server.test.mjs`
- `test/management-dashboard-build.test.mjs`
- `test/management-dashboard-checks.test.mjs`
- `test/management-dashboard-schema.test.mjs`
- `docs/management/dashboard/README.md`
- `docs/management/plans/2026-09-08-control-center.md`
- `README.md`
- `package.json`

## Tests

- Non-loopback configuration rejection.
- Exact request `Host` validation against `127.0.0.1` and the actual listening port.
- Direct ambiguous, malformed, encoded-traversal, backslash, dotfile, and null-byte path rejection.
- GET/HEAD semantics and explicit HTML, JavaScript, CSS, and JSON MIME allowlist.
- Mutation-method rejection and generic error responses without filesystem-path disclosure.
- Realpath containment and escaping-symbolic-link rejection.
- CSP, no-store, nosniff, referrer, cross-origin, opener, and permissions response headers.
- Generated artifact do-not-edit metadata and provenance completeness.
- Operations-guide commands checked against package scripts.
- Dashboard tests included in both `npm test` and the evidence runner's fixed unit command.
- Final management suite passed 42/42; focused server/build suite passed 12/12.

## Validation

The live server returned `200` on `127.0.0.1:4181`, emitted the required security headers, served schema-versioned JSON, and appeared in `lsof` only on exact IPv4 loopback. Quick evidence completed with 8 `PASS`, 0 `FAIL`, and 4 explicit `NOT_RUN`; its old object identifier is intentionally omitted after the public-history remediation.

## Known limitations

- The local runtime observed during this task remained below the repository's declared minimum versions.
- Browser/screen visual inspection was not performed because the user explicitly prohibited screen use.
- Manager, Worker A, decision, work-queue, and changelog sources remain absent and therefore explicit `NOT_AVAILABLE`.
- The loopback server is a foreground developer process, not a persistent operating-system service.

## Not fully resolved

- B8 full project gate, full evidence including the network-dependent audit, security diff review, and final handoff.
- Regeneration of the committed snapshot after this B7 record lands.
- SSH authorized-key and Remote Login cleanup decision from the cancelled B3 workstream.
- Final merge into the default branch requires explicit user approval.

## Deferred work

- Persistent service installation, LAN exposure, authentication, API endpoints, proxying, and remote hosting are intentionally outside scope.
- Browser visual QA remains deferred while the no-screen instruction is active.

## Blockers

`NONE` for B8 validation. Missing optional management sources remain visible but do not block static generation.

## Residual risks

- Any local process running as the same user may read files that the user account can access; the server's path controls do not form an operating-system isolation boundary.
- Redaction and the secret baseline are bounded defenses and cannot prove arbitrary source prose contains no unknown credential format.
- A committed snapshot and its evidence are point-in-time records and become stale after source changes until explicitly regenerated.
- The branch still depends on three prerequisite commits that are not on `origin/master`.

## Decisions

- Require the request `Host` to equal `127.0.0.1:<actual-port>` in addition to binding the socket to loopback.
- Serve only GET/HEAD files beneath the real dashboard root with an explicit MIME and size allowlist.
- Mark generated JSON with machine-readable `doNotEdit`, generator command, and source metadata.
- Run `management:check` in the main gate without running the mutating evidence collector.
- Include every management-dashboard regression in both `npm test` and recorded unit evidence.
- Keep the local preview at port 4181, separate from the project demo at 4180.

## Commit

`SELF` — resolve with `git log -1 --format=%H -- docs/management/tasks/B7.md` after commit.

## Next recommended action

Commit these operational records, rerun evidence on that exact commit, regenerate the snapshot, then execute B8 full validation and the security diff scan before pushing the handoff branch.
